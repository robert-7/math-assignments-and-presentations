# Solving Linear Relation using Linear Algebra Relevant Code

Set up your virtualenv for Linux:

```bash
sudo apt install python3-tk python3-venv
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Run script like so:

```bash
python validate.py
```
